#ifndef TIME_HELPER
#define TIME_HELPER

byte setCurrentTimeInSeconds(unsigned long timestampSeconds);
unsigned long currentTimeSeconds();

//byte setCurrentTime(byte year, byte month, byte date, byte hours, byte minutes, byte seconds);
//void getCurrentTime(byte timebuf[]);

#endif
